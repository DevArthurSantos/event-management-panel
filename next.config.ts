/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    unoptimized: true,
  },
  basePath: "/event-management-panel",
  assetPrefix: "/event-management-panel/",
};

module.exports = nextConfig;